$color = rgba(10,10,12,1.0)
$inner_color = rgba(10,10,12,0.92)
$outer_color = rgba(201,205,196,1.0)
$font_color = rgba(201,205,196,1.0)
$check_color = rgba(31,191,97,1.0)

label {
    monitor =
    text = PLAYER 1 — PRESS START
    font_family = Share Tech Mono
    font_size = 28
    color = rgba(201,205,196,1.0)
    position = 0, 140
    halign = center
    valign = center
}

label {
    monitor =
    text = MEMORY CARD SLOT 1 — CHECKING...
    font_family = Share Tech Mono
    font_size = 13
    color = rgba(110,116,123,1.0)
    position = 0, -110
    halign = center
    valign = center
}
